# Database (local development)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=obifunds
DB_PASSWORD=O0762910601k@?
DB_NAME=obifunds

# Payment gateway (local dev only — production uses live wallet by default)
IOTEC_SANDBOX=true
